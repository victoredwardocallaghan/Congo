
module ARFCNManager where


-- setPower(int dB)
-- sendCommand("SETPOWER", dB)

-- setTSC(unsigned TSC)
-- sendCommand("SETTSC", TSC)

-- setBSIC(unsigned BSIC)
-- sendCommand("SETBSIC", BSIC)

-- setSlot(unsigned TN, unsigned combination)
-- char paramBuf[MAX_UDP_LEN]
-- sprintf(paramBuf, "%d %d", TN, combination)
-- sendCommand("SETSLOT", paramBuf)

-- setMaxDelay(unsigned km)
-- sendCommand("SETMAXDLY", km)

-- setRxGain(signed rxGain)
-- sendCommand("SETRXGAIN", rxGain)

-- setHandover(unsigned TN)
-- sendCommand("HANDOVER", TN)

-- clearHandover(unsigned TN)
-- sendCommand("NOHANDOVER", TN)

-- setTxAtten(signed txAtten)
-- sendCommand("SETTXATTEN", txAtten)

-- setFreqOffset(signed offset)
-- snedCommand("SETFREQOFFSET", offset)

-- getNoiseLevel()
-- sendCommand("NOISELEV", noiselevel)
-- return noiselevel

-- runCustom(const char * command)
-- sendCommand("CUSTOM", command)

-- getFactoryCalibration(const char * param)
-- sendCommand("READFACTORY", param, value)
-- return value

-- receiveBurst(const RxBurst &inBurst)
-- ???
